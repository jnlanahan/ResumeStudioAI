import {
  AnthropicError
} from "./chunk-UOD5AXXY.js";
import "./chunk-G3PMV62Z.js";

// node_modules/@anthropic-ai/sdk/internal/node.browser.mjs
function unavailable(module) {
  return new Proxy({}, {
    get(_target, property) {
      if (typeof property === "symbol")
        return void 0;
      throw new AnthropicError(`\`${module}.${property}\` is not available in this environment; it needs a Node.js-compatible runtime`);
    }
  });
}
var child_process = unavailable("child_process");
var crypto = unavailable("crypto");
var fs = unavailable("fs");
var os = unavailable("os");
var path = unavailable("path");
var stream = unavailable("stream");
var util = unavailable("util");
export {
  child_process,
  crypto,
  fs,
  os,
  path,
  stream,
  util
};
//# sourceMappingURL=node.browser-7NHW5P57.js.map
